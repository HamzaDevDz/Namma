import { createSlice } from '@reduxjs/toolkit';

const initialState = {

};

export const uploadDressSlice = createSlice({
    name: 'uploadDress',
    initialState,
    reducers: {},
    extraReducers: {},
});

// export const {} = uploadDressSlice.actions;


export default uploadDressSlice.reducer;
